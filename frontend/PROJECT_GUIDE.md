# InfoMate 프로젝트 완전 학습 가이드

이 문서는 InfoMate 프로젝트의 모든 내용을 단계별로 설명합니다.

---

## 📚 목차

1. [프로젝트 개요](#1-프로젝트-개요)
2. [프로젝트 구조](#2-프로젝트-구조)
3. [기술 스택](#3-기술-스택)
4. [핵심 파일 설명](#4-핵심-파일-설명)
5. [데이터 흐름](#5-데이터-흐름)
6. [자료구조와 알고리즘](#6-자료구조와-알고리즘)
7. [실행 방법](#7-실행-방법)

---

## 1. 프로젝트 개요

### 무엇을 만드는가?
**InfoMate**는 가짜뉴스를 AI로 판별하는 웹 애플리케이션입니다.

### 주요 기능
1. **뉴스 URL 입력** → 백엔드로 전송
2. **AI 분석** → 백엔드에서 가짜뉴스 여부 판단
3. **결과 표시** → 신뢰도 점수, 분석 내역 표시
4. **기록 저장** → 분석한 뉴스 기록 저장 및 조회

---

## 2. 프로젝트 구조

```
InfoMate/
├── src/
│   ├── components/          # Vue 컴포넌트 (화면)
│   │   ├── Home.vue        # 메인 페이지 (URL 입력)
│   │   ├── AnalysisResult.vue  # 분석 결과 페이지
│   │   └── History.vue     # 평가 기록 페이지
│   ├── services/           # 서비스 (비즈니스 로직)
│   │   ├── api.js          # 백엔드 API 통신
│   │   └── historyService.js  # 기록 관리
│   ├── utils/              # 유틸리티 (도구 함수)
│   │   ├── algorithms.js   # 정렬, 탐색 알고리즘
│   │   ├── dataStructures.js  # 해시테이블, 그래프
│   │   └── urlParser.js    # URL 검증 및 파싱
│   ├── App.vue             # 루트 컴포넌트
│   ├── main.js             # 진입점 (라우터 설정)
│   └── style.css           # 전역 스타일
├── package.json            # 프로젝트 설정
└── vite.config.js         # 빌드 도구 설정
```

---

## 3. 기술 스택

### 프론트엔드
- **Vue 3**: 화면 구성 (컴포넌트 기반)
- **Vue Router**: 페이지 이동 (라우팅)
- **Vite**: 개발 서버 및 빌드 도구

### 백엔드 (연동)
- **Django**: Python 웹 프레임워크
- **PyTorch**: AI 모델 (가짜뉴스 판별)
- **BeautifulSoup**: 웹 크롤링

---

## 4. 핵심 파일 설명

### 4.1 `src/main.js` - 애플리케이션 진입점

**역할**: Vue 앱을 시작하고 라우터를 설정합니다.

```javascript
// 3개의 페이지 경로 설정
const routes = [
  { path: '/', component: Home },              // 메인 페이지
  { path: '/analysis', component: AnalysisResult },  // 분석 결과
  { path: '/history', component: History }    // 기록 보기
]
```

**이해하기**:
- 사용자가 `/` 경로로 접속 → `Home.vue` 표시
- 사용자가 `/analysis` 경로로 접속 → `AnalysisResult.vue` 표시
- 사용자가 `/history` 경로로 접속 → `History.vue` 표시

---

### 4.2 `src/components/Home.vue` - 메인 페이지

**역할**: 뉴스 URL을 입력받고 분석을 시작합니다.

**주요 기능**:
1. **URL 입력**: 사용자가 뉴스 링크 입력
2. **URL 검증**: 입력한 URL이 유효한지 확인
3. **백엔드 호출**: `api.js`를 통해 백엔드에 요청
4. **결과 페이지 이동**: 분석 결과 페이지로 이동

**코드 흐름**:
```javascript
async analyzeNews() {
  // 1. 입력 검증
  if (!this.newsUrl.trim()) {
    alert('뉴스 링크를 입력해주세요.')
    return
  }

  // 2. URL 정규화 (http:// 자동 추가)
  this.newsUrl = normalizeURL(this.newsUrl)

  // 3. URL 검증
  if (!validateURL(this.newsUrl)) {
    alert('유효한 URL을 입력해주세요.')
    return
  }

  // 4. 백엔드 API 호출
  const result = await analyzeNews(this.newsUrl)

  // 5. 기록 저장
  historyService.addRecord({
    url: this.newsUrl,
    data: result.data
  })

  // 6. 결과 페이지로 이동
  this.$router.push({
    path: '/analysis',
    query: { url: this.newsUrl },
    state: { analysisResult: result.data }
  })
}
```

**이해하기**:
- `v-model="newsUrl"`: 입력 필드와 데이터를 연결
- `@click="analyzeNews"`: 버튼 클릭 시 함수 실행
- `isLoading`: 로딩 중인지 표시
- `error`: 에러 메시지 표시

---

### 4.3 `src/services/api.js` - 백엔드 API 통신

**역할**: 백엔드 서버와 통신합니다.

**주요 기능**:
1. **POST 요청**: 뉴스 URL을 백엔드로 전송
2. **응답 처리**: 백엔드 응답을 프론트엔드 형식으로 변환
3. **캐싱**: 같은 URL 재요청 방지 (해시 테이블 사용)
4. **에러 처리**: 에러 메시지 추출 및 표시

**코드 흐름**:
```javascript
export const analyzeNews = async (url, useCache = true) => {
  // 1. 캐싱 확인 (해시 테이블 사용 - O(1))
  if (useCache) {
    const cachedRecord = historyService.getRecordByURL(url)
    if (cachedRecord) {
      return { success: true, data: cachedRecord.data }
    }
  }

  // 2. 백엔드에 POST 요청
  const response = await fetch(`${API_BASE_URL}/`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ url })
  })

  // 3. 응답 처리
  const data = await response.json()
  
  // 4. 백엔드 응답 형식에 맞게 변환
  return {
    success: true,
    data: {
      reliability_score: calculateReliabilityScore(data),
      is_fake: determineIsFake(data),
      metadata: { ... }
    }
  }
}
```

**이해하기**:
- `fetch()`: 브라우저 내장 함수로 HTTP 요청
- `async/await`: 비동기 처리 (백엔드 응답 대기)
- `JSON.stringify()`: JavaScript 객체 → JSON 문자열
- `JSON.parse()`: JSON 문자열 → JavaScript 객체

---

### 4.4 `src/services/historyService.js` - 기록 관리

**역할**: 분석한 뉴스 기록을 저장하고 관리합니다.

**주요 기능**:
1. **기록 저장**: 로컬 스토리지에 저장
2. **빠른 조회**: 해시 테이블로 O(1) 조회
3. **검색**: 키워드로 기록 검색
4. **정렬**: 날짜, 신뢰도 점수로 정렬

**코드 구조**:
```javascript
class HistoryService {
  constructor() {
    // 해시 테이블: URL을 키로 사용
    this.urlHashTable = new HashTable(100)
    // 기록 배열
    this.history = []
    // 로컬 스토리지에서 불러오기
    this.loadHistory()
  }

  // 기록 추가 (해시 테이블 사용 - O(1))
  addRecord(analysisResult) {
    const existingRecord = this.urlHashTable.get(url)
    if (existingRecord) {
      // 기존 기록 업데이트
    } else {
      // 새 기록 추가
      this.urlHashTable.set(url, newRecord)
      this.history.push(newRecord)
    }
    this.saveHistory()
  }

  // 기록 조회 (해시 테이블 사용 - O(1))
  getRecordByURL(url) {
    return this.urlHashTable.get(url)
  }
}
```

**이해하기**:
- **해시 테이블**: URL을 키로 사용하여 빠르게 조회 (O(1))
- **로컬 스토리지**: 브라우저에 데이터 영구 저장
- **중복 검사**: 같은 URL이 이미 있으면 업데이트

---

### 4.5 `src/utils/dataStructures.js` - 자료구조

**역할**: 해시 테이블과 그래프 자료구조를 구현합니다.

#### 해시 테이블 (HashTable)

**용도**: URL을 키로 사용하여 기록을 빠르게 조회

**동작 원리**:
```javascript
// 1. URL을 해시 함수로 인덱스 변환
hash("https://example.com") → 42

// 2. 해당 인덱스의 버킷에 저장
buckets[42] = [{ key: "https://example.com", value: record }]

// 3. 조회 시 해시 함수로 바로 찾기
get("https://example.com") → buckets[42]에서 찾기
```

**시간 복잡도**:
- 조회: O(1) - 평균적으로 매우 빠름
- 삽입: O(1)
- 삭제: O(1)

#### 그래프 (Graph)

**용도**: 관련 기사 추천 기능에 사용

**동작 원리**:
```javascript
// 1. 기사를 노드(정점)로 추가
graph.addVertex("기사1")
graph.addVertex("기사2")

// 2. 관련 기사를 간선(엣지)으로 연결
graph.addEdge("기사1", "기사2", similarity)

// 3. DFS로 관련 기사 탐색
graph.getConnectedVertices("기사1", depth)
```

**시간 복잡도**:
- DFS/BFS: O(V + E) - V: 노드 수, E: 간선 수

---

### 4.6 `src/utils/algorithms.js` - 알고리즘

**역할**: 정렬, 탐색, 문자열 알고리즘을 구현합니다.

#### 퀵 정렬 (Quick Sort)

**용도**: 기록을 날짜나 점수로 정렬

**동작 원리**:
1. 피벗(기준값) 선택
2. 피벗보다 작은 값 → 왼쪽
3. 피벗보다 큰 값 → 오른쪽
4. 재귀적으로 정렬

**시간 복잡도**: O(n log n) - 평균적으로 빠름

#### 머지 정렬 (Merge Sort)

**용도**: 안정적인 정렬이 필요할 때

**시간 복잡도**: O(n log n) - 항상 일정

#### 이진 탐색 (Binary Search)

**용도**: 정렬된 배열에서 빠르게 찾기

**시간 복잡도**: O(log n) - 매우 빠름

#### 선형 탐색 (Linear Search)

**용도**: 정렬되지 않은 배열에서 찾기

**시간 복잡도**: O(n)

---

### 4.7 `src/utils/urlParser.js` - URL 파싱

**역할**: URL을 검증하고 파싱합니다.

**주요 함수**:
1. `validateURL(url)`: URL 유효성 검증
2. `normalizeURL(url)`: URL 정규화 (http:// 자동 추가)
3. `extractDomain(url)`: 도메인 추출
4. `isNewsSite(url)`: 뉴스 사이트 여부 확인

**예시**:
```javascript
normalizeURL("example.com/news")
→ "http://example.com/news"

extractDomain("https://www.bbc.com/korean/articles")
→ "www.bbc.com"

isNewsSite("https://www.bbc.com/korean/articles")
→ true
```

---

## 5. 데이터 흐름

### 전체 흐름도

```
사용자 입력 (Home.vue)
    ↓
URL 검증 (urlParser.js)
    ↓
캐싱 확인 (api.js + historyService.js)
    ↓ (캐시 없으면)
백엔드 API 호출 (api.js)
    ↓
백엔드 응답 처리 (api.js)
    ↓
기록 저장 (historyService.js)
    ↓
결과 페이지 이동 (AnalysisResult.vue)
    ↓
관련 기사 추천 (Graph + DFS)
```

### 상세 흐름

1. **사용자가 URL 입력** (`Home.vue`)
   ```javascript
   newsUrl = "https://example.com/news"
   ```

2. **URL 검증** (`urlParser.js`)
   ```javascript
   validateURL(newsUrl) → true/false
   normalizeURL(newsUrl) → "https://example.com/news"
   ```

3. **캐싱 확인** (`api.js` + `historyService.js`)
   ```javascript
   cachedRecord = historyService.getRecordByURL(url)
   if (cachedRecord) {
     return cachedRecord.data  // API 호출 생략!
   }
   ```

4. **백엔드 API 호출** (`api.js`)
   ```javascript
   fetch('https://backend.com/api/analyze/', {
     method: 'POST',
     body: JSON.stringify({ url })
   })
   ```

5. **백엔드 응답 처리** (`api.js`)
   ```javascript
   {
     publisher_name: "BBC",
     ai_prediction: { fake_probability: 0.2 }
   }
   ↓ 변환
   {
     reliability_score: 80,
     is_fake: false,
     metadata: { publisher: "BBC", ... }
   }
   ```

6. **기록 저장** (`historyService.js`)
   ```javascript
   historyService.addRecord({
     url: "https://example.com/news",
     data: { reliability_score: 80, ... }
   })
   // 해시 테이블에 저장 (O(1))
   // 로컬 스토리지에 저장
   ```

7. **결과 페이지 표시** (`AnalysisResult.vue`)
   - 신뢰도 점수 표시
   - 관련 기사 추천 (Graph + DFS)

---

## 6. 자료구조와 알고리즘

### 왜 사용하는가?

컴퓨터공학과 교육 과정에서 배운 내용을 실제 프로젝트에 적용했습니다.

### 사용 위치

1. **해시 테이블** (`dataStructures.js`)
   - `historyService.js`: URL 기반 빠른 조회 (O(1))
   - `api.js`: 캐싱 기능

2. **그래프** (`dataStructures.js`)
   - `AnalysisResult.vue`: 관련 기사 추천 (DFS)

3. **정렬 알고리즘** (`algorithms.js`)
   - `historyService.js`: 기록 정렬 (퀵 정렬, 머지 정렬)

4. **탐색 알고리즘** (`algorithms.js`)
   - `historyService.js`: 기록 검색 (이진 탐색, 선형 탐색)

5. **문자열 알고리즘** (`algorithms.js`)
   - `urlParser.js`: URL 파싱 및 검증

---

## 7. 실행 방법

### 개발 서버 실행

```bash
# 1. 의존성 설치 (처음 한 번만)
npm install

# 2. 개발 서버 실행
npm run dev

# 3. 브라우저에서 접속
# http://localhost:3000
```

### 빌드 (배포용)

```bash
npm run build
```

---

## 8. 핵심 개념 정리

### Vue.js 기본 개념

1. **컴포넌트**: 화면의 한 부분 (예: 버튼, 입력 필드)
2. **템플릿**: HTML 구조 (`<template>`)
3. **스크립트**: JavaScript 로직 (`<script>`)
4. **스타일**: CSS 스타일 (`<style>`)
5. **데이터 바인딩**: 데이터와 화면 연결 (`v-model`, `{{ }}`)
6. **이벤트 처리**: 사용자 동작 처리 (`@click`, `@keyup`)

### 비동기 처리

```javascript
// async/await 사용
async function fetchData() {
  const response = await fetch(url)  // 응답 대기
  const data = await response.json()  // JSON 변환 대기
  return data
}
```

### 해시 테이블

- **키-값 쌍** 저장
- **빠른 조회** (O(1))
- **중복 검사**에 유용

### 그래프

- **노드(정점)**: 기사
- **간선(엣지)**: 관련성
- **DFS**: 깊이 우선 탐색으로 관련 기사 찾기

---

## 9. 문제 해결 가이드

### 백엔드 연결 오류

1. **CORS 오류**: 백엔드에서 CORS 설정 확인
2. **400 오류**: 요청 형식 확인
3. **500 오류**: 백엔드 코드 오류

### 캐시 문제

- 같은 URL을 다시 분석하면 캐시된 결과 사용
- 강제로 새로 분석하려면: `analyzeNews(url, false)`

---

## 10. 다음 단계

1. **코드 읽기**: 각 파일을 하나씩 읽어보기
2. **디버깅**: 브라우저 개발자 도구로 확인
3. **수정해보기**: 작은 기능 추가해보기
4. **이해하기**: 각 함수의 역할 파악하기

---

## 📝 요약

이 프로젝트는:
- **Vue 3**로 화면 구성
- **백엔드 API**와 통신
- **해시 테이블**로 빠른 조회
- **그래프**로 관련 기사 추천
- **알고리즘**으로 정렬 및 탐색

모든 코드에 상세한 주석이 있으니, 각 파일을 읽어보면서 이해하시면 됩니다!

